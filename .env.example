# Only required if you want to add new job titles 
# Embedding vectors for the default dataset are already pre-calculated and committed.
# To add new titles: rename this file to .env.local and add your key below.

OPENAI_API_KEY=your_openai_api_key_here
